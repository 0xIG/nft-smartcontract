{
  "name": "nft-smartcontract",
  "author": "Grigory Ivanov",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat clean && hardhat compile",
    "compile:force": " hardhat clean && hardhat compile --force",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "check": "hardhat check",
    "coverage": "hardhat clean && hardhat coverage",
    "docs": "hardhat docgen",
    "node": "hardhat node",
    "typechain": "hardhat typechain",
    "prettier": "prettier . --write",
    "prettier:contracts": "prettier --write --plugin=prettier-plugin-solidity 'contracts/**/*.sol'",
    "deploy:factory:sepolia": "hardhat run scripts/deploy/deployFactory.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.5",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomiclabs/hardhat-solhint": "^3.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.18",
    "hardhat-contract-sizer": "^2.10.0",
    "prettier": "^3.2.5",
    "prettier-plugin-solidity": "^1.3.1",
    "solidity-docgen": "^0.6.0-beta.36"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@openzeppelin/contracts-upgradeable": "^5.0.2"
  }
}
